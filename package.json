{
	"name": "llm-relay-api",
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"lint": "tsc --noEmit && biome lint",
		"lint:fix": "tsc --noEmit && biome check --write .",
		"deploy": "vercel deploy . --prod --no-wait"
	},
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"@types/bun": "^1.2.4",
		"@types/node": "^22.13.9",
		"typescript": "^5.8.2",
		"vercel": "^41.3.1",
		"wrangler": "^3.112.0"
	}
}
